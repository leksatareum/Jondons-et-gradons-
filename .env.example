# Copie ce fichier en .env.local et renseigne les deux valeurs.
# Elles se trouvent dans Supabase → Project Settings → API.
# La clé « anon » est publique par nature : c'est la RLS qui protège les
# données, pas le secret de cette clé.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
